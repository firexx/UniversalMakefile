PROJECT=DllProject
SUBPROJECTS=
INCLUDES=
CXXFLAGS_BASE=-c -DNDEBUG -Wall $(INCLUDES) -fPIC
CXXFLAGS_RELEASE= -O3 -DRELEASE
CXXFLAGS_DEBUG  = -O0 -g -DDEBUG
LDFLAGS_BASE=
IGNORE_FILES=
#EXECUTABLE=$(PROJECT)
#LIBRARY=$(addsuffix .a,$(addprefix lib,$(PROJECT)))
DYNAMIC=$(addsuffix .so,$(addprefix lib,$(PROJECT)))

include ../MakefileBase
